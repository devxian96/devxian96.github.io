# DESIGN.md — devxian96.github.io 디자인 시스템 & 서사 구조

> 코드 위치: `src/styles/globals.css`(토큰), `src/app/(main)/components/organisms/*`(섹션), `src/components/molecules/*`(공유 조각).
> 전략·대상·안티레퍼런스는 [PRODUCT.md](./PRODUCT.md).

## 1. 씬(scene)

채용 담당자가 낮에 사무실 모니터로 이력서 PDF와 이 사이트를 나란히 띄워놓고 본다. 주변은 밝고, 다른 지원자 자료와 번갈아 본다.

→ **화이트 기반.** 다크는 이 씬에서 "읽는 문서"가 아니라 "구경하는 데모"로 읽힌다. 다크 토글은 유지하되 기본값은 라이트다.

## 2. 색

**전략: Restrained.** 잉크 계열 뉴트럴 + 단일 액센트.

| 역할        | 값        | 비고                             |
| ----------- | --------- | -------------------------------- |
| 배경        | `#ffffff` | 순백. 크림·샌드 계열 금지        |
| 표면        | `#f4f7fa` | 섹션 교차 밴드, 카드             |
| 잉크 (본문) | `#0e1622` | 대비 16:1                        |
| 잉크 보조   | `#48586c` | 대비 7:1, 회색으로 흐리지 않는다 |
| 액센트      | `#00aaff` | 아래 3곳에만                     |
| 경계        | `#e2e8f0` | 1px                              |

### 액센트 사용처 (Von Restorff — 이 3곳 외 금지)

1. **히어로 마커** — "없으면 만든다"에 형광펜 스와이프
2. **지금 만드는 것** — 현재 진행 중인 한 건의 강조
3. **클로징 마커** — "그래서, 다음은"에 같은 형광펜 (수미상관)

내비게이션 hover, 링크, 포커스 링은 액센트를 쓰되 **채도를 낮춰** 위 3곳의 강조를 잡아먹지 않게 한다.

다크 모드는 토큰만 교체한다. 액센트 규칙은 동일.

## 3. 타이포

| 역할       | 서체                        | 비고                                |
| ---------- | --------------------------- | ----------------------------------- |
| 디스플레이 | Cafe24 Ssurround            | 한글 포함 전 영역. 섹션 제목·히어로 |
| 본문 한글  | Pretendard 400/700          | KS X 1001 서브셋                    |
| 본문 영문  | Cafe24 PRO Slim 300/400/700 | `unicode-range`로 라틴만            |
| 라벨·수치  | 시스템 모노                 | 기간, 연도, 스택                    |

- 디스플레이 스케일: `clamp(2.5rem, 6vw, 5.5rem)`. 상한 6rem 이하.
- 자간 하한 `-0.03em`. 그 아래로 조이지 않는다.
- 본문 `max-width: 68ch`, `text-wrap: pretty`. 제목은 `text-wrap: balance`.
- Cafe24 PRO Slim은 **한글 본문에 쓰지 않는다** — 자소가 눌려 장문 가독성이 떨어진다.

## 4. 서사 구조

하나의 문장을 6번 증명하는 구조다: **없으면 만든다.**

| 순서 | 섹션           | 서사 역할                      | 설득 원리                    |
| ---- | -------------- | ------------------------------ | ---------------------------- |
| 1    | Hero           | 주장 — 나는 없으면 만드는 사람 | 처리 유창성, 단일 CTA(Hick)  |
| 2    | Now            | 지금도 그러고 있다             | 최신성, 구체성               |
| 3    | Work           | 6년치 증거 — 실제 화면과 링크  | Show-don't-tell, 사회적 증거 |
| 4    | Built          | 없어서 만든 부품들             | 일관성 — 주장과 행동의 일치  |
| 5    | Career · Stack | 규모와 범위                    | 밀도, 검증 가능성            |
| 6    | Contact        | 클로징                         | 피크엔드, 수미상관           |

블로그는 6과 5 사이에 얇게 둔다. 채용 담당자에겐 부가 정보, 동료 개발자에겐 진입점.

## 5. 레이아웃

- 콘텐츠 폭 `max-w-6xl`(1152px). 히어로 이미지 밴드와 프로젝트 밴드는 full-bleed.
- 섹션 간 `clamp(5rem, 10vw, 9rem)`. 섹션 내부는 촘촘하게 — 여백은 구조에, 밀도는 내용에.
- **번호 eyebrow(01/02/03) 금지.** 순서가 정보가 아니다. 섹션 제목만으로 위계를 만든다.
- 프로젝트는 **동일 카드 그리드가 아니라 에디토리얼 리스트**. 대표작 3건은 full-bleed 이미지 밴드로, 나머지는 텍스트 행으로 밀도를 준다.
- 사이드 레일 인덱스(데스크탑 ≥1280px)로 현재 위치를 표시한다.

## 6. 모션

- 커브 `cubic-bezier(0.16, 1, 0.3, 1)` (ease-out-expo).
- 진입은 `transform` + `opacity`만. 레이아웃 속성 애니메이션 금지.
- **모든 진입 모션은 이미 보이는 상태를 강화한다.** 초기 `opacity: 0`을 클래스 토글로 걸지 않는다 — 헤드리스 렌더러와 백그라운드 탭에서 빈 화면으로 나간다.
- 히어로 마커는 폭 0 → 100% 스와이프. 클로징 마커도 동일 모션으로 짝을 맞춘다.
- `prefers-reduced-motion: reduce`에서 전부 즉시 표시.

## 7. AI 슬롭 회피 체크리스트

- [ ] 섹션마다 반복되는 번호·대문자 eyebrow 없음
- [ ] 그라데이션 텍스트 없음
- [ ] 동일 규격 카드 그리드 반복 없음
- [ ] 장식용 글래스모피즘 없음
- [ ] `$ whoami` 등 개발자 코스튬 모티프 없음
- [ ] 색 3원칙 위반(액센트 남용) 없음
- [ ] 세지 않은 수치 없음
