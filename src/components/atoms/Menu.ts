import styled from '@emotion/styled';

export const Menu = styled.menu`
    display: flex;
    flex-direction: row;
    justify-content: flex-end;
    align-items: center;
    gap: 1rem;
    list-style: none;
`;
